{
  "name": "@mewmo/ai-workflows",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "cron:ai": "tsx src/commands/run-due.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run src evals --exclude 'dist/**'",
    "eval:offline": "vitest run evals/offline.test.ts",
    "eval:live": "tsx evals/live.ts"
  },
  "dependencies": {
    "tsx": "4.22.4",
    "zod": "4.3.0"
  }
}
