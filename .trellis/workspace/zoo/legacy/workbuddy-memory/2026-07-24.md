# 2026-07-24 工作记录

## Trellis 官方文档 × mewmo 对齐差距分析
- 用户要求拉取 Trellis 官方 quick-start / 架构页并对照 mewmo 出差距清单。
- 拉取并核实了官方四页：`start/install-and-first-task`、`advanced/architecture`、`start/how-it-works`、`advanced/multi-platform`（通过 docs.json 找到真实路由，/zh/architecture 等早期猜测 404）。
- 核心结论：Trellis = Team-level Agent Harness + 内置 LLM Wiki，签名能力是「每轮 workflow-state 注入」（UserPromptSubmit hook 注入当前阶段 breadcrumb）。
- 产出 `TRELLIS-对齐差距清单.md`：分已对齐 / 待补 / 不建议照搬三类。
  - 已对齐：agent.md 冷启动、16 个 .workbuddy/skills、specs/ planning artifact、journal/ + .workbuddy/memory、.claude SessionStart hook、验证纪律、subagent-driven-development、Linear MCP 团队闭环（mewmo 更强）。
  - 最大缺口（P0）：无每轮 workflow-state 注入；mewmo 仅有 SessionStart，无 per-turn guardrail。
  - 待补：task 状态机（建议读 Linear 而非建文件）、JSONL 窄上下文 manifest、finish-work 归档、update-spec 蒸馏、Codex 接线（AGENTS.md+UserPromptSubmit hook）、`.agents/skills/` 共享层。
  - 不建议照搬：agent.md 单文件冷启动被刻意保护不拆分（meta 规则第 3/29 行）；不弱化 Linear 验收闭环；brainstorming 已独立不强制 Superpowers 流水线。
- 建议：保持 mewmo bespoke 三套并行布局（agent.md/.workbuddy/.claude），只补 G1/G2/G3 能力缺口，不引入 `.trellis/` 目录本身。
- 用户当前聚焦选项 A（差距清单），未要求落地改造。

## mewmo → Trellis 完全改造（用户推翻"保持 bespoke"结论）
- 用户明确要求"完全改造成 Trellis"，推翻上午"保持三套并行、不引入 .trellis/"的建议。
- 产出 `TRELLIS-完全改造清单.md`：9 Phase 实施蓝图（Phase 0 边界重写 → 1 骨架 → 5 每轮注入(提前) → 2 spec 拆分 → 3 tasks 迁移+状态机 → 4 workspace 迁移 → 6 skill/sub-agent 对齐 → 7 平台适配 → 8 共享层）。
- **关键决策点（待用户拍板 D1-D4）**：
  - D1：agent.md 第 3/29 行"禁止分层/迁走"硬约束需显式修改（breaking）→ 建议改为 Trellis 式 AGENTS.md 薄索引 + `.trellis/spec/` 模块。
  - D2：task 状态机用 Linear 镜像（hook 注入时查 Linear），不建独立文件状态机，避免双源漂移。
  - D3/D4：统一到 `.agents/skills/` 共享层 + `.trellis/` 事实源，保留 mewmo 已有 16+ skill，仅补 Trellis 5 skill + 3 sub-agent。
- 注意：完全改造会动 agent.md meta 声明，属用户已授权 breaking change，落地 Phase 0 时建议二次确认措辞。

## PR27 生产部署接手（ZOO-64/65/60 集成分支）
- 用户要求"接手 PR27 完成了一半"的任务。PR27 = `integration/ai-retrieval-tools`，集 pgvector 混合检索+rerank+Gemini provider+Jina web 工具+ZOO-60 部署配置。
- 上一 agent 已完成 11 项准备（镜像构建/传输、库 baseline、pgvector 扩展、.env.agent/.env.worker 组装、cron 暂停），剩 A-F 执行步骤全未做。
- 用户授权范围：服务器侧 A/B/C/D+验收 F1 我执行；Vercel 生产变量 E + web 验收 F2 整理清单交付；agent 域名用 `agent.mewmo.zooooo.site`。
- **执行结果**：
  - A 回填：`processed=121 written=0 skipped=121`（旧 BMC relay 数据维度不匹配全 skip，非错误，新内容由 cron 生成）。
  - B agent：`mewmo-agent-agent-1` healthy，`/health`→`{"ok":true}`。
  - C cron：恢复两条，ai-workflows `claimed:0` 链路通、feed `succeeded:12`，新 google/gemini env 生效。
  - D Caddy：append `agent.mewmo.zooooo.site` block 到 `/opt/intro-agent/apps/agent/Caddyfile`（intro-builder 共用，未动现有，已备份 `Caddyfile.pre-mewmo-1784885367`），validate+reload OK；`docker network connect agent_default mewmo-agent-agent-1` 打通容器互访。
  - F1：agent 401 鉴权生效、容器 healthy、cron 正常。证书因 NXDOMAIN 重试中。
- **交付**：`deploy/vercel-env-update.sh`（E 脚本，从 .env.local 读 key 用 `vercel env add --value --force -y`）+ `deploy/PR27-deployment-handoff.md`（DNS 指令/E 手动步骤/F2 web 验收/关键信息/安全约束）。已 gh pr comment 27 同步进展。
- **待用户**：① DNS `agent.mewmo.zooooo.site` A→101.36.117.253；② `bash deploy/vercel-env-update.sh` + DNS 后 `AGENT_SERVER_URL` + `vercel --prod`；③ F2 web 验收。

## D 步骤补全（23:19 用户加完 DNS 后）
- 用户确认 DNS 已加。本机 `dig @8.8.8.8 agent.mewmo.zooooo.site` → `101.36.117.253` 已生效。
- **Caddy 签发坑**：首次 reload 时 DNS 还是 NXDOMAIN，Caddy 把签发排进重试队列（间隔 30~60min），且日志显示新站点证书在从 `acme-staging`（测试环境）签发——根因是 docker volume `agent_caddy_data` 残留 staging 状态（autosave 无显式 acme_ca，但 staging dir 存在）。
- **修复**：在 `agent.mewmo.zooooo.site` block 内显式加 `tls { ca https://acme-v02.api.letsencrypt.org/directory }`（仅影响此站点，不动 deeix/intro-builder），validate+reload 后立刻签发成功。
- **验证**：`openssl` 确认 issuer=Let's Encrypt YE2（真实生产中间证书，非 Fake LE）；`curl -s https://agent.mewmo.zooooo.site/health` → `{"ok":true}` 200（带证书验证通过）。**D 100% 完成**。
- SSH 到 VPS 偶发 `kex_exchange_identification: Connection reset by peer`，加重试循环（最多 5 次 + sleep 8）可稳定进入。
- **剩余**：E（Vercel 生产变量，用户自跑）+ F2（web 验收，用户自跑）。

## E/Vercel 生产部署完成（23:50–00:00）
- 用户跑了 `deploy/vercel-env-update.sh` + `vercel env add AGENT_SERVER_URL ...`，设好 production env（AI_PROVIDER=google、Gemini、Jina rerank、AGENT_INTERNAL_SECRET、AGENT_SERVER_URL=https://agent.mewmo.zooooo.site）。
- **CLI `vercel --prod` 全部失败**：① 默认上传报 `files should NOT have more than 15000 items, received 16721`；② `--archive=tgz` 打成 5.5GB 被 OOM kill（exit 137）。根因：仓库 `.gitignore` 是 `/*`+`!/apps/` 白名单式，本地 `.next`(2.3G)/`node_modules`(1.4G)/`worktree`(1.3G) 被算进上传。加 `.vercelignore` 也未解决（空输出）。
- **正确方法**：mewmo Vercel 项目是 git 集成（push main→生产自动部署，别名 `mewmo-git-main`）。PR27（`integration/ai-retrieval-tools`）CI 全绿、CLEAN、MERGEABLE → `gh pr merge 27 --squash` 合进 main（commit `3262097`），Vercel 自动 git 构建（只取 committed 源码，无 bloat 问题）。
- **结果**：新生产部署 `mewmo-an75jddps` 2 分钟构建 Ready，`https://mewmo.zooooo.site` → HTTP 200。PR27 代码 + 新 env 已上线。
- 教训：本项目生产部署一律走 git merge 到 main，别用 `vercel --prod` CLI 上传本地目录。env-only 变更用 `vercel redeploy`。已写入 MEMORY.md。
