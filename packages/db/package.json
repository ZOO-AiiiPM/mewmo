{
  "name": "@mewmo/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:ensure-development": "tsx scripts/ensure-development-schema.ts",
    "db:ensure-pgvector": "tsx scripts/ensure-pgvector-schema.ts",
    "db:backfill-vectors": "tsx scripts/backfill-embedding-vectors.ts",
    "build": "prisma generate && tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest"
  },
  "dependencies": {
    "@mewmo/shared": "workspace:*",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "7.8.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "prisma": "7.8.0"
  }
}
