# OpenAI API key（必填，否则 AI 助手会提示去配置）
# 复制本文件到 .env 并填入真实 key。.env 已在 .gitignore，不会进仓库。
VITE_OPENAI_API_KEY=

# 可选：自定义 baseURL（走中转 / 自建网关时用）。不填走 https://api.openai.com/v1
# VITE_OPENAI_BASE_URL=https://your-relay.example.com/v1

# 可选：默认模型。不填用 gpt-4.1-mini
# VITE_OPENAI_MODEL=gpt-4.1-mini
