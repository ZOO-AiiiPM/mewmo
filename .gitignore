# 主仓库只 track app/，其他全部不进 git
#
# 元层（CLAUDE.md / journal / specs / lessons / .specify / .claude / 等）
# 物理上呆在主目录作为所有 worktree 的公共父目录访问，
# 但不被主仓库 git 跟踪 —— 协作元层不污染产品代码 commit history。

/*

!/.gitignore
!/app/
